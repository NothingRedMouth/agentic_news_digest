I'll fix README.md later

Launching/Deploying:
1) docker build -t news-digest-container .
2) docker run -d --name agentic-news-digest --gpus all news-digest-container

Will convert everything to docker-compose later
